/* fazer contagem regressiva */

for(let regressiva = 10; regressiva >= 0;
regrssiva -- { (() => {
    console.log(regressiva)
    }, 1000);

    setTimeOut( () => {
    console.log("FELI ANO NOVO !!!")  
         }, (10 - REGRESSIVA) * 1000);
        }